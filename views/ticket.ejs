<!DOCTYPE html>
<html lang="en">
<head>
    <title>Your Pi Day Ticket</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .ticket-container {
            position: relative;
            display: inline-block;
            text-align: center;
            width: 100%;
            max-width: 500px;
        }
        .ticket-image {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }
        .ticket-number {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 5vw;
            font-weight: bold;
            color: #ffffff;
            background: rgba(0, 0, 0, 0.7);
            padding: 2vw 4vw;
            border-radius: 5px;
            white-space: nowrap;
        }
        button {
            background: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            font-size: 4vw;
        }
        button:hover {
            background: #0056b3;
        }
        @media (max-width: 600px) {
            .ticket-number {
                font-size: 6vw;
                padding: 3vw 5vw;
            }
            button {
                font-size: 5vw;
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="ticket-container">
        <img src="a.jpg" alt="Pi Day Ticket" class="ticket-image">
        <p class="ticket-number"><%= ticketNumber %></p>
    </div>
    <button onclick="window.print()">Download Ticket</button>
</body>
</html>
