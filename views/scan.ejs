<!DOCTYPE html>
<html lang="en">
<head>
    <title>Scan QR Code</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: url('b.jpg') no-repeat center center/cover;
            padding: 50px;
            color: white;
        }
        .form-container {
            background: rgba(0, 0, 0, 0.8);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0px 0px 20px rgba(255, 215, 0, 0.8);
            display: inline-block;
            text-align: center;
        }
        input {
            padding: 12px;
            margin: 10px 0;
            width: 90%;
            border: 1px solid #ffc107;
            border-radius: 5px;
            font-size: 16px;
            text-align: center;
        }
        button {
            background: #ffc107;
            color: black;
            border: none;
            padding: 12px 25px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0px 0px 10px rgba(255, 215, 0, 0.7);
        }
        button:hover {
            background: #ffdd57;
            box-shadow: 0px 0px 20px rgba(255, 215, 0, 1);
        }
        .error {
            color: #ff4444;
            margin-top: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h2>Enter Your CID</h2>
    
    <% if (errorMessage) { %>
        <p class="error"><%= errorMessage %></p>
    <% } %>

    <div class="form-container">
        <form id="cidForm" action="/scan" method="POST">
            <input type="text" id="cid" name="cid" placeholder="Enter 11-digit CID" required pattern="\d{11}" title="Must be exactly 11 digits">
            <button type="submit">Get Ticket</button>
        </form>
    </div>

    <script>
        document.getElementById("cidForm").addEventListener("submit", function(event) {
            const cidInput = document.getElementById("cid").value;

            // Ensure CID is exactly 11 digits
            if (!/^\d{11}$/.test(cidInput)) {
                alert("❌ Invalid CID! It must be exactly 11 digits.");
                event.preventDefault();
            }
        });
    </script>
</body>
</html>